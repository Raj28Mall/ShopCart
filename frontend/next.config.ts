/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["placeholder.pics", "image.uniqlo.com", 'www.amazon.in'], // Add the required domain here
  },
};

module.exports = nextConfig;
