/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["placeholder.pics", "image.uniqlo.com", 'www.amazon.in', 'picsum.photos'], // Added picsum.photos domain
  },
};

module.exports = nextConfig;
